#include <iostream>
#include "_vector.h"
#include <vector>

int main() {
    // std::vector<int> v2;
    // for (size_t i = 0; i < 19; ++i) {
    //     v2.push_back(i);
    //     std::cout << v2.size() << " " << v2.capacity() << std::endl;
    // }
    // v2.resize(18);
    // std::cout << "after resize(): " << v2.size() << " " << v2.capacity() << std::endl; 
    // for (size_t i = 0; i < 10; ++i) std::cout << v2[i] << " ";
    // std::cout << std::endl;
    // for (auto x : v2) std::cout << x << " ";
    // std::cout << std::endl;
    // std::cout << std::endl;
    // std::cout << std::endl;

    // int i = int();
    // std::cout << i << std::endl;

    neu3n0::vector<int> v;
}